{
  "name": "statusoverview-cf",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "^22"
  },
  "dependencies": {
    "@statusoverview-cf/cap": "file:cap"
  },
  "devDependencies": {
    "mbt": "^1.2.31",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build:module": "npm-run-all --parallel build:module:cap",
    "build:module:cap": "cd cap && npm run build",
    "build:cf": "mbt build -p=cf",
    "deploy:cf": "cf deploy mta_archives/statusoverview_1.0.0.mtar --retries 1",
    "undeploy:cf": "cf undeploy statusoverview --delete-service-keys --delete-services --delete-service-brokers"
  }
}
