_schema-version: 3.3.0
ID: statusoverview
version: 1.0.0
description: Status Overview
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npm run build:module

modules:
  - name: statusoverview-srv
    type: nodejs
    path: cap/gen/srv
    deployed-after:
      - bc_so-db-deployer
    parameters:
      buildpack: nodejs_buildpack
      disk-quota: 512M
      memory: 512M
    build-parameters:
      before-all:
        - builder: custom
          commands:
            - npm ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: bc_so-auth
      - name: bc_so-dest
      - name: bc_so-db

  - name: bc_so-db-deployer
    type: hdb
    path: cap/gen/db
    build-parameters:
      builder: npm
      ignore:
        - node_modules/
    parameters:
      buildpack: nodejs_buildpack
      disk-quota: 512M
      memory: 512M
    requires:
      - name: bc_so-db

  - name: bc_so-dest-content
    type: com.sap.application.content
    requires:
      - name: bc_so-dest
        parameters:
          content-target: true
      - name: srv-api
      - name: bc_so-auth
        parameters:
          service-key:
            name: bc_so-auth-key
      - name: bc_so-repo-host
        parameters:
          service-key:
            name: bc_so-repo-host-key
    parameters:
      content:
        subaccount:
          destinations:
            - Name: bc_so-srv
              Authentication: OAuth2UserTokenExchange
              TokenServiceInstanceName: bc_so-auth
              TokenServiceKeyName: bc_so-auth-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: com.hugoboss.bc.statusoverview
              HTML5.DynamicDestination: true
              WebIDEEnabled: true
              WebIDEUsage: odata_gen
          existing_destinations_policy: update
        instance:
          destinations:
            - Name: bc_so-srv-api
              Authentication: OAuth2UserTokenExchange
              TokenServiceInstanceName: bc_so-auth
              TokenServiceKeyName: bc_so-auth-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: com.hugoboss.bc.statusoverview
              HTML5.Timeout: 60000
            - Name: bc_so-repo-host
              ServiceInstanceName: bc_so-repo-host
              ServiceKeyName: bc_so-repo-host-key
              sap.cloud.service: com.hugoboss.bc.statusoverview
            - Name: bc_so-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: bc_so-auth
              ServiceKeyName: bc_so-auth-key
              sap.cloud.service: com.hugoboss.bc.statusoverview
          existing_destinations_policy: update
    build-parameters:
      no-source: true

  - name: bc_so-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: bc_so-repo-host
        parameters:
          content-target: true
          service-key:
            name: bc_so-repo-host-key
    build-parameters:
      build-result: resources
      requires:
        - name: statusoverview
          target-path: resources/
          artifacts:
            - statusoverview.zip
        - name: statusoverviewadmin
          target-path: resources/
          artifacts:
            - statusoverviewadmin.zip

  - name: statusoverview
    type: html5
    path: cap/app/statusoverview
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []

  - name: statusoverviewadmin
    type: html5
    path: cap/app/statusoverviewadmin
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []

resources:
  - name: bc_so-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
      config:
        schema: BC_SO_${space}
      service-keys:
        - name: bc_so-db-key

  - name: bc_so-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./cap/xs-security.json
      config:
        xsappname: bc_so-${space}
        tenant-mode: dedicated
      service-keys:
        - name: bc_so-auth-key

  - name: bc_so-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-keys:
        - name: bc_so-repo-host-key

  - name: bc_so-dest
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service-keys:
        - name: bc_so-dest-key
